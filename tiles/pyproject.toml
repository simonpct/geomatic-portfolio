[project]
name = "micromapping-tiles"
version = "0.1.0"
description = "Pipeline de génération de tuiles vectorielles pour le projet micromapping OSM"
requires-python = ">=3.11"
dependencies = [
  "geopandas>=1.0",
  "shapely>=2.0",
  "numpy>=1.26",
  "pyogrio>=0.7",
]
